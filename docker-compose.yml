version: "3.9"

services:
  backend:
    build: ./backend
    container_name: password-backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: password-frontend
    ports:
      - "3000:80"
    restart: always
